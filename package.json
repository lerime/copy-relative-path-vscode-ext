{
  "name": "copy-relative-path",
  "displayName": "Copy Relative Path",
  "description": "Copy Relative Path with Line Number like Jetbrains IDEs",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.testClick",
        "title": "Test Click"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "extension.testClick",
          "group": "9_cutcopypaste"
        }
      ]
    }
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "20.x",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.4.0"
  }
}